# 生产级 AI 对话 CLI 鲁棒性增强 TODO

## 第一优先级：从 demo 变成可用工具

- [x] 配置校验
  - [x] 启动时检查 `OPENAI_API_KEY` 是否存在
  - [x] 启动时检查 `OPENAI_BASE_URL` 是否存在
  - [x] 启动时检查 `MODEL_NAME` 是否存在或设置默认值
  - [x] 检查 `temperature` 等参数是否合法
  - [x] 缺少配置时给出清晰错误提示，不等到第一次请求才失败

- [ ] 精细异常处理
  - [ ] 区分 API Key 错误
  - [ ] 区分模型名称错误
  - [ ] 区分网络连接失败
  - [ ] 区分代理配置错误
  - [ ] 区分请求超时
  - [ ] 区分 429 限流
  - [ ] 区分 5xx 服务端错误
  - [ ] 避免只使用笼统的 `except Exception`

- [ ] 超时控制
  - [ ] 设置连接超时
  - [ ] 设置读取超时
  - [ ] 避免一次请求长时间卡死
  - [ ] 超时后给出可理解的提示

- [ ] 自动重试
  - [ ] 对网络波动进行重试
  - [ ] 对 429 限流进行重试
  - [ ] 对 5xx 服务端错误进行重试
  - [ ] 设置最大重试次数，例如 3 次
  - [ ] 使用递增等待时间，例如 1s、2s、4s
  - [ ] 不对 API Key 错误、模型名错误、参数错误进行重试

- [ ] 上下文长度管理
  - [ ] 限制最大历史轮数
  - [ ] 保留 system prompt
  - [ ] 裁剪过早的 user/assistant 历史消息
  - [ ] 避免 `messages` 无限增长
  - [ ] 后续可扩展为自动总结历史

- [ ] 基础命令系统
  - [ ] `/help`：显示可用命令
  - [ ] `/exit`：退出程序
  - [ ] `/clear`：清空当前上下文
  - [ ] `/model`：显示当前模型
  - [ ] `/config`：显示当前非敏感配置

## 第二优先级：提升真实使用体验

- [ ] 流式输出
  - [ ] 支持模型边生成边显示
  - [ ] 避免用户长时间等待完整回答
  - [ ] 流式输出结束后再把完整回答加入 `messages`

- [ ] 会话持久化
  - [ ] 创建 `sessions/` 目录
  - [ ] 自动保存当前会话
  - [ ] 支持保存为 JSONL
  - [ ] 支持导出为 Markdown
  - [ ] 支持恢复最近一次会话

- [ ] 日志系统
  - [ ] 记录请求时间
  - [ ] 记录模型名称
  - [ ] 记录错误类型
  - [ ] 记录重试次数
  - [ ] 避免记录 API Key
  - [ ] 避免在日志中泄露完整请求头

- [ ] Token 与费用统计
  - [ ] 读取 prompt tokens
  - [ ] 读取 completion tokens
  - [ ] 读取 total tokens
  - [ ] 每轮显示或记录 token 使用量
  - [ ] 后续支持估算费用

- [ ] 输入清洗与编码处理
  - [ ] 清理非法 Unicode 字符
  - [ ] 处理空输入
  - [ ] 处理超长输入
  - [ ] 处理控制字符
  - [ ] 兼容 Windows/WSL 终端编码差异

- [ ] 优雅退出
  - [ ] 捕获 `KeyboardInterrupt`
  - [ ] 捕获 `EOFError`
  - [ ] Ctrl+C 时不显示 traceback
  - [ ] 退出前提示是否保存会话或自动保存

## 第三优先级：工程化结构

- [ ] 拆分项目结构
  - [ ] `config.py`：读取和校验配置
  - [ ] `client.py`：封装模型 API 调用
  - [ ] `chat.py`：管理对话主循环
  - [ ] `commands.py`：处理 `/clear`、`/help` 等命令
  - [ ] `storage.py`：保存和加载会话
  - [ ] `utils.py`：文本清洗、通用工具函数

- [ ] 安全处理
  - [ ] 确认 `.env` 已加入 `.gitignore`
  - [ ] 错误提示中不输出 API Key
  - [ ] 日志中不保存敏感信息
  - [ ] 提醒用户会话历史可能包含隐私内容

- [ ] 测试
  - [ ] 测试配置缺失时的错误提示
  - [ ] 测试 `/clear` 是否能清空上下文
  - [ ] 测试非法 Unicode 是否不会导致程序崩溃
  - [ ] 测试空输入是否会跳过
  - [ ] 测试模型调用失败时是否给出清晰提示
  - [ ] 测试会话保存和加载是否正常

## 建议实现顺序

- [x] 第 1 步：配置校验
- [ ] 第 2 步：基础命令系统
- [ ] 第 3 步：精细异常处理
- [ ] 第 4 步：超时与重试
- [ ] 第 5 步：上下文长度管理
- [ ] 第 6 步：流式输出
- [ ] 第 7 步：会话保存
- [ ] 第 8 步：日志和 token 统计
- [ ] 第 9 步：项目结构拆分
- [ ] 第 10 步：补充测试
